interface:
  display_name: "Fit Flow"
  short_description: "Repo-specific patterns and workflows for fit-flow"
  default_prompt: "Use the fit-flow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true